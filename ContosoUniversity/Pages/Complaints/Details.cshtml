@page
@model ContosoUniversity.Pages.Complaints.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Complaint</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Complaint.Details)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Complaint.Details)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Complaint.Student)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Complaint.Student.FirstMidName)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.Complaint?.ID">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
